
const inquirer = requirer("inquirer");
//import connection
const db = require('./config/connection')

//prompt user which action they want to take
//array of choices: view all departments, view all roles, view all employees, add a department, add a role, add an employee, and update an employee role

//use conditional statement (if or switch)
//choice is iew all departments: execute viewAllDepartment function

function viewAllDepartment() {
    //db.query
}